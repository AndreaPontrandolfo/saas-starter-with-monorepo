{
  "name": "@saas-starter-with-monorepo/db",
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc",
    "publint": "publint",
    "db:push": "drizzle-kit push",
    "db:generate": "drizzle-kit generate",
    "db:studio": "drizzle-kit studio",
    "db:migrate": "drizzle-kit migrate",
    "clean": "rm -rf .turbo dist node_modules/.cache"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.8",
    "@types/pg": "catalog:",
    "typescript": "catalog:",
    "@saas-starter-with-monorepo/config": "workspace:*"
  },
  "dependencies": {
    "drizzle-orm": "catalog:",
    "pg": "^8.16.3",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@saas-starter-with-monorepo/env": "workspace:*"
  }
}
