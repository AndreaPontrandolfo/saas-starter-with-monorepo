{
  "name": "@saas-starter-with-monorepo/config",
  "description": "Configuration templates.",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf .turbo dist node_modules/.cache"
  }
}
